<p style="color: green"><%= notice %></p>
<% content_for :title, "Tasks" %>
<div id="tasks" class="mt-8 mb-8 ">
  <% @tasks.each do |task| %>
    <%= render task %>
  <% end %>
</div>
<%= link_to "New task", new_task_path %>
